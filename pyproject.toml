[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "steward-by-aftergraph"
version = "0.2.0.dev0"
description = "STEWARD composition and integration root for Aftergraph"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
